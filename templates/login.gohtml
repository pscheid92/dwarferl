{{define "content"}}
    <h1>Login Page</h1>
    {{ if .userID }}
        <p>Hello {{ .userID }}!</p>
        <a href="{{$.linkPrefix}}logout/google">Logout</a>
    {{ else }}
        <a href="{{$.linkPrefix}}auth/google">Login with Google</a>
    {{ end }}
{{end}}

{{template "base" .}}
